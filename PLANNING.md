# 유튜브 댓글 이벤트 추첨 시스템 기획안

## 프로젝트 개요

**프로젝트명**: Yupick (Youtube Pick)
**목적**: 유튜브 영상의 댓글을 분석하여 구독자 중 특정 조건에 맞는 참가자를 추첨하는 자동화 시스템
**작성일**: 2025-12-08
**구현 방식**: 전통적인 알고리즘 기반 (LLM/AI 불필요, 저비용 운영)

### 핵심 특징

- ✅ **LLM 불필요**: 전통적인 문자열 매칭 알고리즘만으로 구현
- ✅ **저비용 운영**: 추가 API 비용 없음 (YouTube API만 사용)
- ✅ **빠른 처리**: 복잡한 모델 추론 없이 즉시 처리
- ✅ **투명한 로직**: 명확한 규칙 기반 필터링

---

## 1. 프로젝트 배경

유튜브 채널 운영자가 이벤트를 진행할 때, 댓글을 수동으로 확인하고 추첨하는 과정은 시간이 많이 소요되고 공정성 문제가 발생할 수 있습니다. 본 시스템은 다음과 같은 문제를 해결합니다:

- 많은 댓글 중 조건에 맞는 참가자를 자동으로 필터링
- 구독자 확인 자동화
- 투명하고 공정한 랜덤 추첨 프로세스
- 이벤트 진행 시간 단축

---

## 2. 핵심 기능 요구사항

### 2.1 구독자 필터링 기능

**설명**: 댓글 작성자 중 채널 구독자만 선별

**세부 요구사항**:

- YouTube Data API를 통한 댓글 수집
- 댓글 작성자의 구독 여부 확인
- 구독자가 아닌 사용자 자동 제외
- 구독 확인 실패 시 로깅 및 예외 처리

**기술적 고려사항**:

- YouTube Data API v3 사용
- API 할당량 관리 (하루 10,000 units 기본)
- 대량의 댓글 처리를 위한 배치 처리

### 2.2 아이템 필터링 기능

**설명**: 댓글에서 특정 아이템/키워드를 언급한 참가자만 선별

**세부 요구사항**:

- 관리자가 지정한 아이템 키워드 목록
- 댓글 텍스트에서 키워드 매칭
- 대소문자 무시 검색
- 부분 일치 및 완전 일치 옵션
- 여러 아이템 중 하나 이상 언급 시 조건 충족
- 정규표현식 지원 (선택적)

**예시**:

```
아이템 목록: ["에어팟", "아이패드", "맥북"]
댓글: "에어팟 당첨되고 싶어요!" → 필터 통과
댓글: "당첨되면 좋겠어요!" → 필터 미통과
```

**기술적 고려사항**:

- 한글, 영문 등 다국어 지원
- 이모지 및 특수문자 처리
- 띄어쓰기 변형 처리 (예: "에어팟", "에어 팟")

**오타 처리 전략**:

사용자들의 다양한 오타와 표기 변형을 처리하기 위한 다층 필터링 시스템입니다.
**중요**: 모든 처리는 전통적인 문자열 알고리즘으로 구현 가능하며, LLM이나 AI 모델이 필요하지 않습니다.

1. **텍스트 정규화 (1차 처리)**

   - 공백 제거: "에어 팟" → "에어팟"
   - 대소문자 통일: "AirPod" → "airpod"
   - 특수문자 제거: "에어팟!" → "에어팟"
   - 중복 문자 정리: "에어팟팟" → "에어팟"

2. **동의어/별칭 사전**

   ```
   에어팟: ["에어팟", "airpod", "에어팟프로", "에어팟 프로", "무선이어폰"]
   아이패드: ["아이패드", "ipad", "아이 패드", "태블릿"]
   맥북: ["맥북", "macbook", "맥 북", "맥북프로", "맥북에어"]
   ```

   - 관리자가 각 아이템별 허용 키워드 목록 설정
   - 영문/한글 표기 모두 포함

3. **퍼지 매칭 (Fuzzy Matching)**

   - Levenshtein Distance 알고리즘 사용
   - 유사도 임계값 설정 (예: 80% 이상 일치)
   - 오타 예시:
     - "에어팟" ↔ "에어포드" (편집 거리: 1, 유사도: 85%)
     - "아이패드" ↔ "아이팻" (편집 거리: 1, 유사도: 87%)
     - "맥북" ↔ "맥붘" (편집 거리: 1, 유사도: 75%)

4. **한영 오타 처리**

   - 한글 키보드로 영어 입력 오류
     - "airpod" (영문) → "ㅁㅑㄱㅔㅐㅇ" (한글 자판)
   - 자모 분리 및 재조합 알고리즘
   - 양방향 변환 검사

5. **초성 검색 (선택적)**

   - "ㅇㅇㅍ" → "에어팟" 매칭
   - 한글 초성 추출 및 비교
   - 모호성이 높아 옵션으로 제공

6. **유사도 임계값 설정**

   ```
   - 완전 일치: 100% (우선순위 1)
   - 동의어 일치: 100% (우선순위 2)
   - 퍼지 매칭: 80-99% (우선순위 3, 관리자 설정 가능)
   - 초성 일치: 70-79% (우선순위 4, 선택적)
   ```

7. **잘못된 매칭 방지**
   - 너무 짧은 키워드 제외 (2글자 미만)
   - 일반 명사 필터링 ("상품", "선물" 등)
   - 블랙리스트 키워드 설정

**구현 우선순위**:

- Phase 1 (MVP): 정규화 + 동의어 사전
- Phase 2: 퍼지 매칭
- Phase 3: 한영 오타 + 초성 검색

**예시 시나리오**:

```
설정된 아이템: "에어팟"
동의어: ["에어팟", "airpod", "에어팟프로"]

댓글: "에어팟 갖고싶어요" → ✅ 완전 일치
댓글: "에어 팟 주세요" → ✅ 정규화 후 일치
댓글: "AirPod 당첨!" → ✅ 동의어 일치
댓글: "에어포드 받고싶어요" → ✅ 퍼지 매칭 (유사도 85%)
댓글: "에어팟츠 주세요" → ⚠️ 퍼지 매칭 (유사도 75%, 임계값에 따라)
댓글: "에어컨 있어요" → ❌ 유사도 낮음
```

**관리자 인터페이스**:

- 매칭 임계값 조정 슬라이더
- 동의어 사전 관리 UI
- 오탐지/미탐지 댓글 리뷰 기능
- 필터링 결과 미리보기

### 2.3 랜덤 추첨 기능

**설명**: 필터링된 참가자 중 무작위로 당첨자 선정

**세부 요구사항**:

- 암호학적으로 안전한 난수 생성기 사용
- 추첨 인원 수 설정 가능
- 중복 당첨 방지 옵션
- 추첨 시드(seed) 저장으로 재현 가능성 확보
- 추첨 이력 저장 및 감사 추적

**추첨 알고리즘**:

- Fisher-Yates 셔플 알고리즘
- 각 참가자에게 동등한 당첨 확률 보장
- 타임스탬프 및 랜덤 시드 기록

**공정성 검증**:

- 추첨 과정 로그 저장
- 추첨 결과 재검증 기능
- 투명한 알고리즘 공개

### 2.4 필터링 결과 검토 및 수동 관리 시스템

**설명**: 자동 필터링 결과를 관리자가 검토하고 수정할 수 있는 시스템

**필요성**:

- 자동 필터링은 완벽하지 않을 수 있음
- 오탐지(false positive): 아이템을 언급했지만 걸러진 경우
- 미탐지(false negative): 아이템을 언급하지 않았지만 통과한 경우
- 경계선 케이스(borderline cases): 유사도가 애매한 경우
- 관리자의 최종 판단이 필요한 경우

#### 2.4.1 댓글 분류 시스템

**댓글 상태 분류**:

```
1. 자동 통과 (Auto-Approved)
   - 완전 일치 또는 동의어 일치
   - 높은 신뢰도 (95% 이상)
   - 색상: 초록색

2. 수동 승인 (Manually Approved)
   - 관리자가 직접 추가한 댓글
   - 색상: 진한 초록색

3. 보류 (Pending Review)
   - 퍼지 매칭으로 걸린 경우 (75-95%)
   - 관리자 검토 필요
   - 색상: 노란색

4. 자동 거부 (Auto-Rejected)
   - 아이템 키워드 없음
   - 낮은 유사도
   - 색상: 회색

5. 수동 거부 (Manually Rejected)
   - 관리자가 직접 제외한 댓글
   - 색상: 빨간색

6. 재검토 요청 (Flagged for Review)
   - 애매한 케이스
   - 색상: 주황색
```

#### 2.4.2 댓글 보관 데이터베이스 구조

**Comments 테이블 확장**:

```sql
comments:
  - id: 댓글 고유 ID
  - event_id: 이벤트 ID
  - comment_id: YouTube 댓글 ID
  - author_name: 작성자 이름
  - author_channel_id: 작성자 채널 ID
  - text: 댓글 내용
  - published_at: 작성 시간

  # 필터링 관련
  - filter_status: 필터링 상태 (auto_approved, manually_approved, pending, auto_rejected, manually_rejected, flagged)
  - matching_method: 매칭 방법 (exact, synonym, fuzzy, manual)
  - similarity_score: 유사도 점수 (0-100)
  - matched_keyword: 매칭된 키워드
  - matched_item: 매칭된 아이템

  # 구독자 확인
  - is_subscriber: 구독자 여부
  - subscriber_check_status: 확인 상태 (verified, failed, pending)

  # 추첨 관련
  - is_eligible: 최종 추첨 대상 여부
  - eligibility_reason: 자격 여부 사유

  # 관리자 메모
  - admin_notes: 관리자 메모
  - reviewed_by: 검토한 관리자 ID
  - reviewed_at: 검토 시간
  - modified_at: 마지막 수정 시간
```

**Filter History 테이블** (검토 이력):

```sql
filter_history:
  - id: 이력 ID
  - comment_id: 댓글 ID
  - event_id: 이벤트 ID
  - action: 액션 (approved, rejected, flagged, unflagged)
  - previous_status: 이전 상태
  - new_status: 새로운 상태
  - reason: 변경 사유
  - admin_id: 관리자 ID
  - timestamp: 변경 시간
```

#### 2.4.3 관리자 검토 인터페이스

**1. 필터링 결과 대시보드**

```
┌─────────────────────────────────────────────┐
│  📊 필터링 결과 요약                          │
├─────────────────────────────────────────────┤
│  전체 댓글: 1,247개                           │
│  ✅ 자동 통과: 456개 (36.6%)                  │
│  ⚠️  보류: 128개 (10.3%)  ← 검토 필요!        │
│  ❌ 자동 거부: 663개 (53.1%)                  │
└─────────────────────────────────────────────┘
```

**2. 댓글 리스트 뷰** (탭 구분)

```
[전체] [✅ 통과] [⚠️ 보류] [❌ 거부] [🔍 수동 처리]

필터: [상태] [유사도] [작성자] [날짜]
검색: [댓글 내용 검색...]                   [일괄 작업 ▼]

┌────────────────────────────────────────────────────────────────┐
│ ⚠️  홍길동 (@user123) | 유사도: 82% | 작성: 2025-12-08 10:23    │
│ "에어포드 너무 갖고 싶어요! 구독했습니다!"                          │
│ 매칭: "에어포드" ≈ "에어팟" (퍼지 매칭)                            │
│ [✅ 승인] [❌ 거부] [🔍 보류] [📝 메모 추가]                       │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│ ⚠️  김철수 (@user456) | 유사도: 78% | 작성: 2025-12-08 10:25    │
│ "맥북에어 주세요!!"                                               │
│ 매칭: "맥북에어" ≈ "맥북" (퍼지 매칭)                             │
│ [✅ 승인] [❌ 거부] [🔍 보류] [📝 메모 추가]                       │
└────────────────────────────────────────────────────────────────┘
```

**3. 상세 검토 모달**

```
┌──────────────────────────────────────────────────────────────┐
│  댓글 상세 검토                                        [X 닫기] │
├──────────────────────────────────────────────────────────────┤
│  작성자: 홍길동 (@user123)                                     │
│  작성일: 2025-12-08 10:23:45                                  │
│  구독자: ✅ 확인됨                                             │
│                                                                │
│  댓글 내용:                                                    │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ "에어포드 너무 갖고 싶어요! 구독했습니다!"                │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                                │
│  필터링 분석:                                                  │
│  • 매칭 방법: 퍼지 매칭 (Fuzzy Matching)                      │
│  • 입력 텍스트: "에어포드"                                     │
│  • 타겟 아이템: "에어팟"                                       │
│  • 유사도 점수: 82%                                           │
│  • 편집 거리: 1                                               │
│                                                                │
│  관리자 판단:                                                  │
│  ○ 추첨 대상에 포함                                           │
│  ○ 추첨 대상에서 제외                                         │
│  ○ 나중에 다시 검토                                           │
│                                                                │
│  메모 (선택):                                                  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 오타로 보임. 승인 처리.                                  │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                                │
│  [이전 댓글] [다음 댓글]          [저장] [취소]                │
└──────────────────────────────────────────────────────────────┘
```

**4. 일괄 처리 기능**

```
☑️ 선택된 댓글 15개

[일괄 작업 ▼]
  ├─ ✅ 모두 승인
  ├─ ❌ 모두 거부
  ├─ 🔍 보류로 변경
  ├─ 📤 CSV 내보내기
  └─ 🗑️ 삭제
```

#### 2.4.4 검색 및 필터링 기능

**고급 필터**:

- 상태별 필터 (통과/보류/거부)
- 유사도 범위 (예: 70-85%)
- 매칭 방법별 (완전일치/동의어/퍼지/수동)
- 구독자 여부
- 작성 날짜 범위
- 작성자 검색
- 댓글 내용 키워드 검색

**정렬 옵션**:

- 유사도 낮은 순 (검토 우선순위)
- 작성 시간 순
- 작성자 이름 순
- 상태별

#### 2.4.5 수동 추가/제외 기능

**1. 수동으로 댓글 추가**

```
시나리오: 자동 필터링에서 걸러졌지만 실제로는 포함되어야 하는 경우

[거부된 댓글] 탭에서:
┌────────────────────────────────────────────────────────────────┐
│ ❌ 박영희 (@user789) | 유사도: 0% | 작성: 2025-12-08 11:00      │
│ "당첨되고 싶어요! 꼭 필요해요!" (키워드 없음)                       │
│ [➕ 강제 추가] [📝 메모]                                          │
└────────────────────────────────────────────────────────────────┘

관리자 판단: "댓글에는 키워드가 없지만, 작성자가 별도로 요청함"
→ [강제 추가] 클릭 → 추첨 대상에 포함
```

**2. 수동으로 댓글 제외**

```
시나리오: 자동으로 통과했지만 실제로는 제외되어야 하는 경우

[통과된 댓글] 탭에서:
┌────────────────────────────────────────────────────────────────┐
│ ✅ 이철수 (@user999) | 유사도: 100% | 작성: 2025-12-08 11:30    │
│ "에어팟은 별로예요. 안 갖고 싶어요."                               │
│ [🗑️ 강제 제외] [📝 메모]                                         │
└────────────────────────────────────────────────────────────────┘

관리자 판단: "키워드는 있지만 부정적 의미로 사용됨"
→ [강제 제외] 클릭 → 추첨 대상에서 제외
```

#### 2.4.6 패턴 분석 및 제안 시스템

**통계 기반 피드백 루프** (LLM 불필요):

- 관리자의 승인/거부 빈도를 카운트 (단순 통계)
- 자주 승인되는 오타 패턴을 동의어 사전에 자동 추가 제안
- 자주 거부되는 패턴을 블랙리스트에 추가
- **구현**: 간단한 빈도수 계산 및 임계값 비교

**통계 데이터 활용**:

```
예시:
- "에어포드" → 10번 승인됨
  → 시스템 제안: "에어포드"를 "에어팟" 동의어로 추가하시겠습니까?

- "에어컨" → 8번 거부됨
  → 시스템 제안: "에어컨"을 블랙리스트에 추가하시겠습니까?
```

#### 2.4.7 통계 및 리포트

**필터링 품질 리포트**:

```
┌─────────────────────────────────────────────────────────────┐
│  📈 필터링 정확도 분석                                        │
├─────────────────────────────────────────────────────────────┤
│  자동 필터링 정확도: 87.3%                                    │
│  수동 개입 필요: 12.7% (128건)                                │
│                                                               │
│  수동 승인 (오탐지): 45건                                     │
│  수동 거부 (미탐지): 83건                                     │
│                                                               │
│  평균 검토 시간: 댓글당 8초                                   │
│  총 검토 시간: 17분 4초                                       │
└─────────────────────────────────────────────────────────────┘
```

**자주 발생하는 오타 TOP 10**:

```
1. "에어포드" (45건) → "에어팟"
2. "맥붘" (23건) → "맥북"
3. "아이팻" (18건) → "아이패드"
...
```

#### 2.4.8 API 엔드포인트 추가

```
# 필터링 결과 조회
GET    /api/events/:id/comments/filtered
  ?status=pending,auto_approved,auto_rejected
  &similarity_min=70
  &similarity_max=90

# 댓글 상태 변경
PATCH  /api/comments/:id/status
  Body: { status: "manually_approved", reason: "오타로 판단", admin_notes: "..." }

# 일괄 상태 변경
PATCH  /api/comments/bulk-update
  Body: { comment_ids: [...], status: "manually_approved" }

# 검토 이력 조회
GET    /api/comments/:id/history

# 필터링 통계
GET    /api/events/:id/filter-stats
```

#### 2.4.9 워크플로우

```
1. 댓글 수집 완료
   ↓
2. 자동 필터링 실행
   ↓
3. 관리자에게 "보류" 댓글 알림
   ├─ 보류 댓글: 128개
   └─ 검토 추천 시간: ~17분
   ↓
4. 관리자 검토
   ├─ 빠른 검토 (유사도 낮은 순)
   ├─ 승인/거부/보류 결정
   └─ 메모 작성 (선택)
   ↓
5. 최종 추첨 대상 확정
   ↓
6. 추첨 실행
```

#### 2.4.10 사용자 경험 개선

**키보드 단축키**:

- `A`: 승인 (Approve)
- `R`: 거부 (Reject)
- `F`: 보류 (Flag)
- `N`: 다음 댓글 (Next)
- `P`: 이전 댓글 (Previous)
- `Space`: 상세 보기

**진행 상황 표시**:

```
검토 진행: ████████░░ 78개/128개 (60.9%)
예상 남은 시간: 약 7분
```

**자동 저장**:

- 모든 변경사항은 즉시 저장
- 네트워크 오류 시 로컬 저장 후 재시도

---

## 3. 시스템 아키텍처

### 3.1 기술 스택 (제안)

**백엔드**:

- Node.js / Python (선택)
- YouTube Data API v3
- 데이터베이스: SQLite / PostgreSQL
- **필터링 엔진**:
  - 정규표현식 (내장)
  - Levenshtein Distance 라이브러리 (예: `fuzzywuzzy`, `difflib`)
  - 한글 처리: `hangul-js` (Node.js) 또는 `hgtk` (Python)
  - **주의**: LLM/AI 모델 불필요, 추가 API 비용 없음

**프론트엔드 (웹 인터페이스)**:

- React / Vue.js
- 관리자 대시보드
- 추첨 결과 시각화

**배포**:

- Docker 컨테이너화
- 클라우드 호스팅 (AWS, GCP, Azure) 또는 로컬 실행
- **비용**: YouTube API 할당량만 관리하면 됨 (무료 할당량 내 사용 가능)

### 3.2 시스템 구성도

```
[YouTube API]
     ↓
[댓글 수집 모듈]
     ↓
[구독자 확인 모듈] ← [YouTube API]
     ↓
[아이템 필터링 모듈]
     ↓
[랜덤 추첨 모듈]
     ↓
[결과 저장 및 출력]
```

---

## 4. 주요 워크플로우

### 4.1 이벤트 생성

1. 관리자가 이벤트 정보 입력
   - 영상 URL
   - 아이템 키워드 리스트
   - 당첨 인원 수
   - 추첨 옵션 (중복 허용 여부 등)

### 4.2 데이터 수집

1. YouTube API를 통해 댓글 수집
2. 댓글 작성자 정보 저장
3. 중복 댓글 제거 (같은 사용자의 여러 댓글 처리)

### 4.3 필터링 단계

1. **1차 필터**: 구독자 여부 확인

   - API 호출로 각 사용자의 구독 상태 확인
   - 구독자만 다음 단계로 진행

2. **2차 필터**: 아이템 키워드 매칭
   - 댓글 텍스트에서 지정된 키워드 검색
   - 조건에 맞는 댓글만 선별

### 4.4 추첨

1. 필터링된 참가자 목록 생성
2. 랜덤 추첨 알고리즘 실행
3. 당첨자 선정

### 4.5 결과 출력

1. 당첨자 목록 표시
   - 채널명
   - 댓글 내용
   - 작성 시간
2. 추첨 로그 저장
3. 결과 내보내기 (CSV, JSON 등)

---

## 5. 데이터 모델

### 5.1 이벤트 (Events)

```
- id: 이벤트 고유 ID
- video_url: 유튜브 영상 URL
- video_id: 유튜브 영상 ID
- items: 아이템 키워드 배열
- winner_count: 당첨 인원 수
- allow_duplicate: 중복 당첨 허용 여부
- created_at: 이벤트 생성 시간
- drawn_at: 추첨 실행 시간
- status: 이벤트 상태 (준비중/진행중/완료)
```

### 5.2 댓글 (Comments)

```
- id: 댓글 고유 ID
- event_id: 이벤트 ID (외래키)
- comment_id: YouTube 댓글 ID
- author_name: 작성자 이름
- author_channel_id: 작성자 채널 ID
- text: 댓글 내용
- published_at: 작성 시간
- is_subscriber: 구독자 여부
- item_matched: 매칭된 아이템
- is_eligible: 추첨 대상 여부
```

### 5.3 당첨자 (Winners)

```
- id: 당첨 고유 ID
- event_id: 이벤트 ID (외래키)
- comment_id: 댓글 ID (외래키)
- rank: 당첨 순위
- drawn_at: 추첨 시간
- seed: 추첨 시드값
```

---

## 6. API 설계 (REST API)

### 6.1 이벤트 관리

```
POST   /api/events          - 이벤트 생성
GET    /api/events          - 이벤트 목록 조회
GET    /api/events/:id      - 이벤트 상세 조회
PUT    /api/events/:id      - 이벤트 수정
DELETE /api/events/:id      - 이벤트 삭제
```

### 6.2 댓글 수집

```
POST   /api/events/:id/collect    - 댓글 수집 시작
GET    /api/events/:id/comments   - 수집된 댓글 조회
```

### 6.3 필터링

```
POST   /api/events/:id/filter     - 필터링 실행
GET    /api/events/:id/eligible   - 추첨 대상자 조회
```

### 6.4 추첨

```
POST   /api/events/:id/draw       - 추첨 실행
GET    /api/events/:id/winners    - 당첨자 조회
GET    /api/events/:id/export     - 결과 내보내기
```

---

## 7. UI/UX 설계

### 7.1 주요 화면

1. **대시보드**

   - 전체 이벤트 목록
   - 진행 상태별 필터링

2. **이벤트 생성 화면**

   - 영상 URL 입력
   - 아이템 키워드 입력 (다중 입력)
   - 추첨 옵션 설정

3. **이벤트 상세 화면**

   - 댓글 수집 진행 상황
   - 필터링 결과 통계
   - 추첨 대상자 목록

4. **추첨 화면**
   - 추첨 버튼
   - 당첨자 발표 애니메이션
   - 결과 다운로드

### 7.2 사용자 경험

- 진행 상태를 실시간으로 표시 (프로그레스 바)
- 각 단계별 결과 미리보기
- 오류 발생 시 명확한 에러 메시지
- 반응형 디자인 (모바일 대응)

---

## 8. 보안 및 개인정보 보호

### 8.1 API 키 관리

- 환경 변수를 통한 API 키 관리
- .env 파일 사용 및 .gitignore 설정
- API 키 암호화 저장

### 8.2 개인정보 처리

- 필요 최소한의 정보만 수집
- 개인정보 보관 기간 설정
- GDPR 및 개인정보보호법 준수
- 당첨자 정보 익명화 옵션

### 8.3 접근 제어

- 관리자 인증 시스템
- 이벤트별 권한 관리
- API 레이트 리미팅

---

## 9. 제약사항 및 고려사항

### 9.1 YouTube API 제약

- **할당량 제한**: 하루 10,000 units
  - 댓글 조회: 1 unit
  - 구독 확인: 1 unit
- **처리 전략**:
  - 배치 처리로 API 호출 최소화
  - 캐싱 전략 도입
  - 할당량 초과 시 재시도 로직

### 9.2 대량 댓글 처리

- 1만 개 이상의 댓글 처리 시 성능 최적화 필요
- 페이지네이션 및 비동기 처리
- 데이터베이스 인덱싱

### 9.3 구독자 확인의 한계

- YouTube API의 구독자 확인 기능 제한
- 채널이 구독 목록을 비공개로 설정한 경우 확인 불가
- 대안: 댓글 작성자가 직접 구독 인증 스크린샷 제출

---

## 10. 개발 단계

### Phase 1: MVP (최소 기능 제품)

- [ ] YouTube API 연동
- [ ] 댓글 수집 기능
- [ ] 아이템 필터링 기능
- [ ] 랜덤 추첨 기능
- [ ] CLI 인터페이스

### Phase 2: 웹 인터페이스

- [ ] 관리자 대시보드
- [ ] 이벤트 생성/관리 UI
- [ ] 추첨 결과 시각화

### Phase 3: 고급 기능

- [ ] 구독자 확인 자동화
- [ ] 중복 댓글 처리
- [ ] 다중 언어 지원
- [ ] 통계 및 분석 기능

### Phase 4: 최적화 및 배포

- [ ] 성능 최적화
- [ ] 에러 핸들링 강화
- [ ] 문서화 및 사용자 가이드
- [ ] 배포 자동화

---

## 11. 테스트 계획

### 11.1 단위 테스트

- 각 모듈별 기능 테스트
- 필터링 로직 검증
- 추첨 알고리즘 공정성 검증

### 11.2 통합 테스트

- API 연동 테스트
- 전체 워크플로우 테스트

### 11.3 성능 테스트

- 대량 댓글 처리 성능 측정
- API 할당량 관리 검증

### 11.4 사용자 테스트

- 실제 사용 시나리오 테스트
- UI/UX 피드백 수집

---

## 12. 성공 지표

- **기능성**: 설정된 조건에 맞는 당첨자 정확한 선정
- **공정성**: 모든 참가자에게 동등한 당첨 확률 보장
- **효율성**: 이벤트 진행 시간 50% 이상 단축
- **신뢰성**: 추첨 과정의 투명성 및 재현 가능성 확보
- **사용성**: 관리자가 5분 이내에 이벤트 생성 및 추첨 완료

---

## 13. 향후 확장 가능성

**기본 기능 확장**:

- 다중 영상 동시 이벤트 지원
- 자동 당첨자 알림 기능 (이메일, DM)
- 이벤트 템플릿 기능
- 추첨 라이브 스트리밍 기능
- 다른 플랫폼 지원 (트위치, 인스타그램 등)
- 통계 대시보드 (참여율, 구독자 증가율 등)

**고급 필터링 (선택적, 추가 비용 발생)**:

- LLM 기반 문맥 이해 (부정적 의미 감지)
- 의미 기반 매칭 (시맨틱 검색)
- **주의**: 현재 기획안의 전통적 알고리즘만으로도 85-90% 이상의 정확도 달성 가능
- LLM 도입은 예산과 필요성에 따라 추후 고려

---

## 14. 참고 자료

**YouTube API**:

- [YouTube Data API v3 Documentation](https://developers.google.com/youtube/v3)
- [YouTube Data API - Comments](https://developers.google.com/youtube/v3/docs/comments)
- [YouTube Data API - Subscriptions](https://developers.google.com/youtube/v3/docs/subscriptions)

**알고리즘 및 라이브러리**:

- Fisher-Yates Shuffle Algorithm (공정한 랜덤 추첨)
- 암호학적 난수 생성 (Crypto.getRandomValues, secrets module)
- Levenshtein Distance (문자열 유사도)
  - Python: `python-Levenshtein`, `fuzzywuzzy`, `difflib`
  - Node.js: `fast-levenshtein`, `fuzzyset.js`, `string-similarity`
- 한글 처리
  - Node.js: `hangul-js`
  - Python: `hgtk`, `jamo`

---

## 15. 문의 및 기여

프로젝트에 대한 문의사항이나 기여 방법은 프로젝트 저장소의 Issue 또는 Pull Request를 통해 참여해주세요.

---

**문서 버전**: 1.0
**최종 수정일**: 2025-12-08

---

## 16. Vercel 원클릭 배포 계획 (Next.js 단일 앱)

본 프로젝트는 **웹 UI가 필요**하며, 가능하면 **프론트엔드 + 백엔드 API를 하나로 묶어** 배포합니다. Vercel 환경과 궁합이 가장 좋은 형태로 **Next.js 단일 앱**으로 구성하고, 백엔드는 **Next.js Route Handlers**(`app/api`)로 구현합니다.

### 16.1 목표 아키텍처

```
Browser
  ↓
Next.js UI (App Router)
  ↓ (same origin)
Next.js API Route Handlers (/api/*)
  ↓
YouTube Data API v3
```

### 16.2 레포/폴더 구조 (권장)

```
Yupick/
  PLANNING.md
  web/
    package.json
    next.config.ts
    .env.local                # 로컬 개발용 (gitignore)
    src/
      app/
        page.tsx              # 대시보드/홈
        events/
          page.tsx            # 이벤트 목록
        events/[id]/
          page.tsx            # 이벤트 상세/필터/추첨
        api/
          events/
            route.ts          # 이벤트 생성/목록 (GET/POST)
          events/[id]/
            route.ts          # 이벤트 상세 (GET/PATCH/DELETE)
          events/[id]/collect/
            route.ts          # 댓글 수집 시작
          events/[id]/filter/
            route.ts          # 필터 실행
          events/[id]/draw/
            route.ts          # 추첨 실행
          events/[id]/winners/
            route.ts          # 당첨자 조회
      lib/
        youtube.ts            # YouTube API 호출 래퍼
        filtering.ts          # 키워드/퍼지매칭 로직
        draw.ts               # 추첨 알고리즘
    prisma/ (선택)
      schema.prisma
```

### 16.3 Vercel 배포 방식

- **프로젝트 타입**: Next.js
- **배포 단위**: `web/` (Vercel Root Directory를 `web`로 지정)
- **Build Command**: `next build` (기본값)
- **Output**: Vercel 기본값 사용
- **API**: `/app/api/**`는 자동으로 Serverless Functions로 배포됨

### 16.4 환경 변수(초기안)

Vercel의 Project Settings → Environment Variables에 등록합니다.

- `YOUTUBE_API_KEY`
- `YOUTUBE_CHANNEL_ID` (운영 채널 ID)
- `NEXT_PUBLIC_APP_URL` (선택: UI에서 절대 URL 필요 시)
- `DATABASE_URL` (DB: Supabase Postgres 연결 URL)
- `DIRECT_URL` (선택 권장: Prisma migrate/seed용 Direct 연결 URL)

로컬 개발 시에는 `web/.env.local`에 등록합니다.

Supabase를 사용할 경우 권장값:

- **`DATABASE_URL`**: Supabase **Pooler(트랜잭션 풀링)** 연결 문자열
  - Vercel(서버리스)에서 커넥션 폭증/타임아웃을 줄이기 위함
- **`DIRECT_URL`**: Supabase **Direct(비풀링)** 연결 문자열
  - `prisma migrate dev` 같은 마이그레이션 작업에서 권장

### 16.5 배포 전 체크리스트

- **레포에 실제 Next.js 앱 코드가 존재** (`web/package.json`, `web/src/app` 등)
- **로컬에서 빌드 성공** (`next build`)
- **YouTube API Key는 절대 코드에 하드코딩하지 않음**
- **API 요청량/쿼터 고려**
  - 댓글 수집/구독자 확인은 배치 처리 + 캐싱 전략 필요
- **장시간 작업 고려**
  - 댓글 수집/필터링이 길어질 경우 동기 API 한 번으로 처리하지 말고
    - 작업을 쪼개거나
    - 이벤트 상태 저장 후 폴링 UI로 진행률 표시

### 16.6 Vercel에 “한 번에 배포” 절차

1. GitHub에 레포 푸시
2. Vercel에서 New Project → 해당 레포 Import
3. Framework Preset: Next.js 확인
4. Environment Variables 등록
5. Deploy

### 16.8 로컬 실행(MVP: 이벤트 생성/목록 + 댓글 수집)

아래 커맨드는 `web/` 폴더 기준입니다.

1. 의존성 설치

   - `npm install`

2. DB 준비(Prisma)

   - `DATABASE_URL` 설정 (Supabase Pooler 권장)
   - `DIRECT_URL` 설정 (Supabase Direct 권장)
   - 초기 마이그레이션 생성/적용: `npx prisma migrate dev`

3. 개발 서버 실행

   - `npm run dev`

4. 동작 확인
   - UI: `/events`
   - API: `GET /api/events`, `POST /api/events`
   - 댓글 수집: `POST /api/events/:id/collect` (서버에서 YouTube API 호출)

### 16.7 운영 시 유의사항

- YouTube API는 **서버에서만 호출**(API Key 노출 방지)
- 구독자 확인은 YouTube API 정책/제약에 따라 불가 케이스가 존재하므로,
  - UI에서 “확인 불가” 상태를 별도 표시하고,
  - 관리자 수동 승인 플로우(기획 2.4)와 연결
